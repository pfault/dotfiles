;;(require 'nexus-magit)

;;(use-package zerodark-theme
;;	     :straight t
;;	     :init
;;	     (load-theme 'zerodark t)
;;	     ;; Optionally setup the modeline
;;	     (zerodark-setup-modeline-format)
;;	     )

;;(provide 'nexus-zerodark-theme)
